# 032 — Technical gate T8, T11, T12 and T13: plan clean, PIP geometry, safe area, budget

## Type

AFK

## Parent PRD

`issues/prd.md`

## What to build

The four plan-and-ledger checks that complete T1–T13. T8: the plan validates with zero unresolved violations and the render log has no NetworkError, and the rescued-beat limit from 016 is reported here. T11: the measured face box sits fully inside the PIP circle on the eight strip frames and the chin is above 90 % of the window. T12: no caption, stamp or lower-third geometry lies inside the 6.3 reserved zones, computed from the render spec, not pixels. T13: the ledger total, per-step caps and the over-soft-cap flag are recorded. Delivery now requires all thirteen.

Covers PRD `qa.technical` T8, T11, T12, T13, `pipeline` `delivered` rule. Decisions 10.1, 3.3, 6.3, 5.6, 11.3, 4.4.

## Acceptance criteria

- [ ] T8: re-validates `plan.validated.json` against the grammar, scans `work/render.log` for `NetworkError`, and fails on more than four rescued beats per 60 s with "not enough relevant B-roll".
- [ ] T11: geometry from `job.json` face box and PIP window; fails naming the frame index where the box leaves the circle or the chin drops below 90 %.
- [ ] T12: iterates every text element in the RenderSpec against top 250, bottom 320, right 140 px; fails naming the element and beat.
- [ ] T13: writes ledger total, per-step totals against the style `budget` allowances and the soft-cap flag into `qa.json`; never fails for cost alone (the hard cap already failed the job earlier if hit).
- [ ] `delivered` requires T1–T13 all `pass` and the four output files; any FAIL stops delivery naming the check.
- [ ] Passing and failing synthetic tests for each check.
- [ ] Smoke asserts all thirteen pass on the fixture.

## Blocked by

- Blocked by `issues/031-gate-t5-t7-t10.md`
- Blocked by `issues/013-presenter-face-measurement.md`

## User stories addressed

- User story 14
- User story 22
- User story 28
- User story 44
