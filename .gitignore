# Python-generated files
__pycache__/
*.py[oc]
build/
dist/
wheels/
*.egg-info

# Virtual environments
.venv

# Secrets and operator-edited config (decision 5.6: prices live in a file, not in code)
.env
prices.yaml

# Media and job data (never commit video)
data/
uploads/
outputs/
work/
*.mp4
*.mov
*.wav
*.mp3
*.m4a

# Node (the Remotion picture engine, ticket 004); its bundle lands under build/
node_modules/

# Local-only tool state
.claude/settings.local.json
reviews/
